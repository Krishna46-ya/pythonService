# ML Service – Aadhaar Fraud Sentinel

FastAPI-based ML service for detecting anomalous activity from CSV uploads.

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
